# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
test
prompt

server.log
# Skills directory
/skills/
# Reference dump (Twenty CRM repo) — keep Turbopack watcher under inotify limits
/reference

# ===== BLINDAJE DE DATOS PRIVADOS (CRM ALBRA) =====
# Base de datos real con clientes — NUNCA commitear
db/
*.db
*.db-journal
*.db-shm
*.db-wal
# Sesión de WhatsApp (Baileys) — credenciales de sesión
.wa-auth/
wa-auth/
# Uploads de usuarios
/upload/
# Artefactos locales de desarrollo
/tool-results/
/repo-compare/
# Plantilla de entorno SÍ se commitea
!.env.example
# Sesión WhatsApp — variantes (daemon en cualquier subcarpeta)
**/.wa-auth/
.wwebjs_auth/
.wwebjs_cache/
# Comprimidos y IDE
*.zip
*.tar.gz
.vscode/
.idea/
Thumbs.db
agent-ctx/
/download/
